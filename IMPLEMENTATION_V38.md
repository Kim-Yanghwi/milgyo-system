# v38 회계 실무관리 배포·운영 안내

## v38.1 화면·사용성 보완

2026년 8월 4일 요청사항에 따라 `/accounting-operations/` 화면을 다음과 같이 보완했습니다.

1. 좌측 메뉴와 회계연도 선택기를 기본 회계 화면과 같은 폭·서체·크기·선택색으로 통일하고, 연도 선택값을 `2026 회계연도` 형식으로 표시
2. 현황 화면의 4단계 업무 카드를 거래 가져오기·자동대사·예산 통제·홈택스 결과 반영 위치와 연결
3. 모든 날짜 입력을 `YYYYMMDD`, `YYMMDD`, `MMDD`, `YYYY-MM-DD` 형식으로 입력할 수 있게 하고, 유효한 날짜는 `YYYY-MM-DD`로 자동 변환한 뒤 괄호 없이 요일 표시
4. 미대사 작업함·월별 대사표·가용예산·승인이력·거래처·계좌변경·계약지급 표에 내부 실선과 정렬 기준 적용
5. 계약 확인 체크박스를 가깝게 배치하고, 전자기부금영수증 일괄발급 선택 체크박스를 15px로 축소

이번 v38.1 보완은 화면 코드만 변경하므로 이미 `0009_v38_finance_operations.sql`을 적용한 운영 DB에 추가 마이그레이션을 실행할 필요가 없습니다.

## 구현 범위

`/accounting-operations/` 화면에 다음 기능을 추가했습니다.

1. 통장·법인카드 CSV/XLSX 가져오기, 중복 차단, 자동분류, 자동대사, 미대사 작업함, 월 대사표
2. 추경·전용·이체·감액·초과집행 예외의 요청/승인, 예산 버전 스냅샷, 계약 약정액 반영 가용예산
3. 거래처 중복검사, 계좌 마스킹·해시, 계좌변경 별도승인, 계약·지급일정·지출결의·전표 연결
4. 전자기부금영수증 홈택스 작업용 Excel 생성, 처리결과 CSV/XLSX 재가져오기, 발급·오류·취소 상태 반영

미대사 거래 또는 월 대사 미확정 계좌가 있으면 해당 월 회계마감이 차단됩니다. 확정된 예산금액도 기존 예산 입력 화면에서 직접 덮어쓸 수 없고 예산 변경요청을 거쳐야 합니다.

## 배포 전 필수 작업

회계 전용 D1에 아래 마이그레이션을 먼저 적용한 뒤 Pages를 배포합니다.

```powershell
npx wrangler d1 migrations apply milgyo-accounting-db --remote
```

적용 대상 신규 파일은 `migrations/accounting/0009_v38_finance_operations.sql`입니다. 운영 DB 적용 전에는 반드시 기존 백업 스크립트로 원격 백업을 생성하십시오.

배포 후에는 다음 순서로 확인합니다.

1. 관리자·회계담당자 계정으로 `/accounting-operations/` 접속
2. 대사 대상 은행계좌 1건 등록
3. 소량의 테스트 CSV를 가져오고 자동대사 실행
4. 미대사 건 처리 후 월 대사표 생성
5. 예산 변경요청을 다른 승인권자 계정에서 승인
6. 테스트 거래처·계약·지급일정 생성 후 기존 지출결의 연결
7. 기부금 1~2건으로 작업용 Excel 생성 및 모의 결과파일 반영
8. 테스트 자료 삭제 후 실제 운영 개시

## 거래 파일 준비 기준

- 첫 행은 열 제목이어야 합니다.
- 거래일자와 금액 열은 필수입니다.
- 입금액·출금액이 분리된 파일과 단일 금액+입출금 구분 파일을 모두 지원합니다.
- 카드 자료는 모든 거래를 출금으로 처리합니다.
- 한 번에 최대 1,000건을 가져옵니다.
- 같은 자료를 다시 가져오면 자료유형·계좌·일자·금액·승인번호·순번 등의 조합으로 중복을 차단합니다.

## 전자기부금영수증 유의사항

생성 파일은 홈택스 업로드를 준비하기 위한 작업파일입니다. 홈택스에서 내려받은 최신 Excel 서식의 열 제목과 순서를 최종 기준으로 삼아야 합니다. 기부자의 주민등록번호·사업자등록번호 원문은 서버에 저장하지 않으므로 작업파일의 빈 식별번호 열을 안전한 환경에서 보완합니다.

결과파일을 다시 가져올 때는 다음 열을 자동 인식합니다.

- 내부관리번호, 기부번호 또는 관리번호
- 처리결과, 결과 또는 상태
- 홈택스 발급번호, 발급번호 또는 영수증번호
- 오류내용, 오류사유, 메시지 또는 비고

## 검증 결과

- `tsc -p tsconfig.check.json`: 통과
- `npm run build`: 통과
- v38.1 화면 요구사항 점검: 4단계 이동 대상, 숫자 날짜 변환 4종, 괄호 없는 요일, 7개 지정 표 실선·정렬, 체크박스 크기 확인
- v38 SQL을 신규 SQLite DB에 적용 후 `PRAGMA integrity_check`: `ok`
- `npm audit --omit=dev`: 공개 취약점 0건
- D1 호환 임시 DB 통합 스모크 테스트: 계좌등록 → 거래가져오기 → 자동대사 → 월대사 → 예산변경승인 → 거래처·계약·지급연결 → 기부금 결과반영 통과

Excel 읽기·쓰기 때문에 `exceljs`가 추가되었습니다. 실무관리 화면의 초기 JavaScript는 약 31KB(압축 약 10KB)이며, 약 940KB의 Excel 처리 모듈은 CSV가 아닌 Excel 파일을 읽거나 생성할 때만 지연 로드됩니다. 기존 Astro 5.10.2의 공개 보안경고를 제거하기 위해 Astro 7.1.6과 안전한 하위 의존성 버전으로 갱신했습니다.
