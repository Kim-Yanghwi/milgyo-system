# 종단관리시스템(system.milgyo.org) 배포 가이드

기존 milgyo-official-site와 완전히 분리된 Cloudflare Pages 프로젝트입니다.
공식 홈페이지 코드와 절대 섞지 마세요(같은 저장소에 넣지 않습니다).

## 이번 업데이트로 바뀐 것

1. **목록 화면 전체 표(table) 재정비** — 결재대기함·발송대기함·문서등록대장·완료문서함·반려문서함뿐 아니라
   접수·발송대장까지 모두 `<table>` 기반으로 통일하고, 화면 가로 폭을 꽉 채우도록 만들었습니다.
   문서 값(구분/보고일자/제목/부서/보고자 등)은 모두 가운데 정렬됩니다.
   **주의**: 이전에도 표 형식 코드는 있었지만 화면에 그대로 반영되지 않았다고 하셨습니다. 이번 파일을
   적용하신 뒤에도 예전과 똑같이 보인다면, 브라우저 강력 새로고침(Ctrl+Shift+R / Cmd+Shift+R)을 먼저
   시도해 주세요 — Cloudflare Pages/브라우저 캐시가 예전 HTML을 붙들고 있는 경우가 많습니다.
2. **관인·로고 등록/고정 기능** — 문서작성 화면 미리보기 위쪽의 "관인·로고 관리" 버튼(관리자 전용)에서
   로고와 관인 이미지를 한 번 등록해 두면, 이후 모든 공문서 미리보기(작성/상세 화면 공통)에 자동으로
   표시됩니다. 이미지가 없으면 예전처럼 문양(卍)·텍스트 관인으로 표시됩니다.
3. **공문서 서식 레이아웃 재설계** — 네이버웍스 공문 미리보기를 참고해 글자 크기와 여백을 줄이고,
   위에는 수신처 정보(수신/경유/제목), 아래에는 발신처 정보(발신명의+관인)를 배치해 한 페이지 안에
   자연스럽게 들어오도록 다시 짰습니다.
4. **문서작성 화면 UX 개편** — 입력 항목을 "①문서 종류 · ②결재라인 지정 · ③수신·본문 · ④붙임파일"
   네 단계 카드로 나누어, 무엇을 입력해야 하는지 순서대로 눈에 들어오게 만들었습니다.
5. **계정관리 표 개선** — 계정 목록에 담당부서 컬럼을 추가하고, 계정정보 항목을 가운데 정렬했습니다.
6. **조직도 기능** — 상단 메뉴의 "계정관리"와 "로그아웃" 사이에 "조직도" 버튼이 새로 생겼습니다.
   왼쪽에 부서 트리(네이버웍스 조직도와 동일한 구성), 오른쪽에 그 부서 소속 인원이 표시됩니다.
   계정관리에서 계정에 담당부서를 지정해 두면 자동으로 이 조직도에 연동됩니다.

## 배포 순서(코드 반영)

GitHub 저장소(`Kim-Yanghwi/milgyo-system`)에서 아래 파일들을 "연필 아이콘(Edit this file) → 전체선택 후
붙여넣기 → Commit" 방식으로 반영해 주세요. 새 파일은 "Add file → Create new file"로 만들면 됩니다.

**교체할 기존 파일**
- `docs/schema.sql`
- `functions/_shared/helpers.ts`
- `functions/api/users/create.ts`
- `functions/api/users/update.ts`
- `functions/api/users/list.ts`
- `functions/api/received/create.ts` (변경 없음 — 그대로 두셔도 됩니다)
- `src/pages/index.astro`
- `wrangler.toml` (database_id가 이미 실제 값으로 채워져 있습니다. 그대로 반영하시면 됩니다.)

**새로 추가할 파일**
- `functions/api/settings/get.ts`
- `functions/api/settings/save.ts`

D1 데이터베이스는 별도 SQL을 다시 실행하실 필요가 없습니다. `functions/_shared/helpers.ts`의
`ensureTables()`가 앱이 실행될 때마다 새 테이블(`org_settings`)과 `system_users` 테이블의 새 컬럼
(`department`)을 자동으로 추가합니다.

## wrangler.toml 관련 주의사항(반복 발생했던 오류)

`wrangler.toml`의 `database_id` 값이 `REPLACE_WITH_ACTUAL_D1_DATABASE_ID` 같은 자리표시자로 되돌아가면
`Error 8000022: Invalid database UUID` 오류가 다시 발생합니다. 이번에 전달드리는 `wrangler.toml`은
실제 값(`8c130839-aca2-4d85-8495-10460e07bbc5`)이 이미 채워져 있으니, GitHub에 반영하신 뒤
Cloudflare Pages **Deployments 목록**에서 새로 초록색으로 뜬 배포가 있는지 확인해 주세요(예전 배포를
다시 실행하지 마시고, 항상 최신 커밋 기준의 새 배포를 확인하셔야 합니다).

## 배포 후 확인 방법

1. 배포가 끝나면 `system.milgyo.org`에 접속해 **강력 새로고침**을 한 번 해 주세요.
2. 로그인 후 결재대기함으로 이동해, 표 테두리가 뚜렷하게 보이고 화면 가로 폭을 꽉 채우는지 확인합니다.
3. 계정관리에서 담당부서를 지정한 계정을 1~2개 만들어 보시고, 상단 "조직도" 버튼을 눌러 해당 인원이
   부서 트리에 나타나는지 확인합니다.
4. 문서작성 화면에서 "관인·로고 관리" 버튼으로 이미지를 등록한 뒤, 미리보기에 반영되는지 확인합니다.

## 이번에는 넣지 않은 것(범위를 벗어난 기능)

- 완성된 공문서를 PDF로 인쇄/다운로드하는 기능은 아직 없습니다(다음 단계로 추가 가능).
- 조직도는 현재 등록된 계정(담당부서가 지정된 사람)만 표시합니다. 계정이 없는 부서는 조직도에서
  "지정된 인원이 없습니다"로 표시됩니다.

## 문서24 연동은 어떻게 되는가
행정기관 공문 송수신 자체는 말씀하신 대로 문서24를 그대로 쓰시면 됩니다. 이 시스템은 문서24를 대체하지
않고, 문서24로 주고받은 문서를 "접수·발송대장"에 수기로 한 줄 등록해 종단 자체 기록으로 남기는 역할만
합니다.
