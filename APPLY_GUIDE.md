# 종단관리시스템 통합 개선본 적용 가이드

대상 저장소: `Kim-Yanghwi/milgyo-system`  
운영 주소: `https://system.milgyo.org`

이 개선본은 공식 홈페이지·회원전용공간·규정정보시스템과 분리된 **내부 전자결재·문서관리 전용 시스템**입니다. 행정기관과의 실제 공문 송수신은 문서24 등을 계속 사용하고, 그 결과를 이 시스템의 접수·발송대장에 수동 등록하는 구조입니다.

## 1. 주요 개선내용

### 화면·문서함
- 홈 대시보드 신설: 결재대기, 진행문서, 임시저장, 발송대기, 완료문서, 접수·발송 건수 표시
- 결재대기함·진행문서함·임시저장함·발송대기함·완료문서함·반려문서함·문서등록대장을 온나라형 가로 테이블로 통일
- 접수·발송대장도 동일한 전체폭 테이블, 검색, 기간조회, 정렬, 페이지 이동 제공
- 좌측 메뉴 접기 버튼을 추가해 넓은 화면에서는 대장을 거의 전체폭으로 사용
- 문서 제목과 주요 열을 가운데 정렬하고 행 간격·경계선·호버 표시 정비
- 검색 결과 전체 CSV 저장(현재 페이지가 아니라 필터에 맞는 전체 결과)

### 내부결재
- 임시저장 → 상신 → 검토대기(선택) → 결재대기 → 승인/반려 흐름
- 중요문서와 경미한 전결문서 분리
- 기안자 회수, 반려문서 복사·재기안, 일괄결재, 결재의견·이력 보관
- 관련자 전용 열람범위 지원
- 임시저장 문서는 작성자와 관리자만 열람하도록 보안 보완
- 발송문서는 승인 후 발송대기함에서 발송완료 처리

### 문서작성·서식
- 거의 전체화면 크기의 문서작성 창
- 좌측 입력폼과 우측 실시간 공문 미리보기 분할
- 입력 라벨·항목을 행 단위로 균일하게 정렬
- 관인·로고 등록 및 공문 미리보기·인쇄 반영
- 관리자용 서식 추가·복사·수정·사용중지 기능
- 서식 항목의 유형, 필수 여부, 전체폭/반폭, 선택값, 입력안내, 기본값 설정
- 기본 제공 서식:
  1. 일반 기안서(내부결재)
  2. 지출품의서
  3. 지출결의서
  4. 회의 개최 계획서
- 공급가액과 부가세 입력 시 합계 자동 계산
- 회계연도 기본값은 고정 연도가 아니라 현재 연도로 자동 입력

### 접수·외부발송 대장
- 문서24·우편·이메일·방문 등 외부에서 들어오거나 나간 문서를 수동 등록
- 상대기관 문서번호, 담당부서, 연결 내부문서, 메모, 원문파일 기록
- 등록자 또는 관리자가 수정·삭제 가능
- 삭제 시 관련 첨부파일도 함께 정리

### 첨부파일 저장
- Cloudflare R2가 `FILES` 이름으로 연결되어 있으면 첨부파일을 R2에 저장
- R2가 없을 때에는 기존 호환을 위해 작은 파일만 D1에 저장
- D1은 행당 2MB 제한이 있으므로 1.2MB를 넘는 파일은 R2 연결이 필수
- 파일당 최대 4MB, 문서당 최대 10개
- HTML·JS·실행파일·스크립트 등 위험한 확장자 차단

### 보안·안정성
- PBKDF2 비밀번호 저장 및 로그인 시도 제한
- 관리자 마스터 키 상수시간 비교
- 첨부파일 확장자·크기·권한 검증
- 임시문서 직접 URL 열람 차단
- 접수·발송 첨부파일을 등록자 또는 관리자만 추가 가능
- 중복 상신 방지용 요청 ID
- 출력 CSV 수식 삽입 방지
- 보안 응답 헤더 및 검색엔진 차단 유지

## 2. 적용 방법

### 가장 안전한 방법
1. 현재 로컬 `milgyo-system` 폴더를 별도 ZIP으로 백업합니다.
2. 이 개선본 ZIP을 풉니다.
3. 개선본의 모든 파일과 폴더를 현재 저장소 최상위에 덮어씁니다.
4. GitHub Desktop에서 변경 파일을 확인합니다.
5. 커밋 문구를 다음과 같이 입력합니다.

```text
종단관리시스템 온나라형 UI 및 내부결재 통합 개선
```

6. `Commit to main` → `Push origin`을 누릅니다.
7. Cloudflare Pages에서 최신 커밋의 배포가 성공했는지 확인합니다.

## 3. D1 설정

현재 `wrangler.toml`의 D1 설정은 다음 이름을 사용합니다.

```toml
binding = "DB"
database_name = "milgyo-system-db"
```

`database_id`는 현재 운영 D1의 실제 ID가 들어 있습니다. 다른 D1을 사용할 경우에만 수정합니다.

기존 D1에는 SQL을 다시 실행하지 않아도 됩니다. Functions의 `ensureTables()`가 다음 항목을 자동 추가합니다.

- 문서서식 테이블·필드
- 접수·발송 등록자 ID 및 수정일
- 첨부파일 저장방식·R2 키 컬럼
- 필요한 인덱스
- 기본 제공 서식 4종

새 D1을 만들 때에는 `docs/schema.sql`을 한 번 실행합니다.

## 4. R2 연결 — 강력 권장

D1에는 최대 행 크기 제한이 있어 PDF·HWP·사진을 안정적으로 보관하려면 R2가 필요합니다.

1. Cloudflare Dashboard → `Storage & databases` → `R2 Object Storage`
2. 버킷 생성: 예시 `milgyo-system-files`
3. `Workers & Pages` → `milgyo-system` → `Settings`
4. `Bindings` → `Add` → `R2 bucket`
5. Variable name: **FILES**
6. R2 bucket: 생성한 `milgyo-system-files`
7. Production에 저장하고 새로 배포합니다.

R2가 연결되지 않으면 홈 화면에 경고가 표시되며, 1.2MB를 넘는 첨부파일은 저장하지 않습니다.

## 5. 운영 보안 설정

시스템 자체 로그인에 더해 Cloudflare Access로 `system.milgyo.org` 전체를 보호하는 것을 권장합니다.

- Access 대상: `system.milgyo.org/*`
- 허용 이메일: 실제 관리자·직원 계정만
- 애플리케이션 로그인 후 시스템 자체 계정으로 한 번 더 로그인
- 관리자 계정 공동사용 금지
- 퇴직·업무변경 시 계정 즉시 비활성화

Cloudflare Pages 환경변수에는 충분히 긴 `ADMIN_TOKEN`을 Secret으로 등록합니다. 최초 관리자 생성 또는 비상 복구 외에는 노출하지 않습니다.

## 6. 배포 후 필수 점검

1. 로그인 후 홈 대시보드가 표시되는지
2. 메뉴 접기 후 목록이 화면 전체폭으로 넓어지는지
3. 임시저장 → 수정 → 상신이 되는지
4. 검토자와 결재자가 순서대로 처리할 수 있는지
5. 기안자가 진행문서를 회수할 수 있는지
6. 승인·반려·완료·문서등록대장에 정확히 분류되는지
7. 기본 서식 4종이 문서작성 화면에 표시되는지
8. 지출품의·결의서의 합계가 자동 계산되는지
9. 관리자 서식관리에서 새 서식을 추가하고 실제 작성할 수 있는지
10. 접수·발송 등록 → 수정 → 상세조회 → 삭제가 되는지
11. R2 연결 후 1.2MB 이상 PDF 첨부·다운로드가 되는지
12. CSV 버튼이 현재 검색조건의 전체 결과를 저장하는지
13. 관인·로고 등록 후 미리보기와 인쇄에 표시되는지
14. 일반 사용자가 다른 사람의 임시저장 문서를 직접 열 수 없는지

## 7. 운영 흐름 권장안

### 내부결재
```text
기안자 작성·임시저장
→ 상신
→ 검토자 검토완료(선택)
→ 최종결재자 승인 또는 반려
→ 완료문서함·문서등록대장 보관
```

### 외부 공문
```text
내부 발송문서 기안·결재
→ 문서24·우편·이메일 등으로 실제 발송
→ 발송완료 처리
→ 접수·발송대장에 외부발송 기록과 원문 등록
```

### 외부 수신문서
```text
문서24·우편·이메일 등으로 수신
→ 접수·발송대장에 접수 등록
→ 원문 첨부
→ 필요한 경우 관련 내부 기안문서 번호 연결
```

## 8. 범위

이 시스템은 온나라 문서관리카드의 핵심 흐름을 참고해 소규모 법인 내부업무에 맞게 단순화한 시스템입니다. 국가 전자문서유통망, 기록관리시스템 이관, 전자관인 인증, 행정전자서명, 문서24 API 직접연계 기능은 포함하지 않습니다. 외부 송수신은 문서24를 사용하고 이 시스템에는 결과를 수동 등록합니다.
