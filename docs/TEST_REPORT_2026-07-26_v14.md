# v14 검사 결과

- Cloudflare Functions TypeScript 정적검사: 통과
- 화면 인라인 JavaScript 문법검사: 통과
- 결재자 지정 화면의 협조자 우선 배치: 통과
- 미리보기 결재선 `기안 → 협조 → 검토 → 결재·전결` 순서 검사: 통과
- 서버 결재선 생성 순서 `협조 → 검토 → 결재·전결` 검사: 통과
- 발신명의 직함·성명 다음 줄 배치 검사: 통과
- 직함·성명 오른쪽 정렬 및 하단 안내문 여백 검사: 통과
- 인쇄/PDF 전용 CSS 동일 반영 검사: 통과

npm 패키지 저장소가 제한시간 안에 응답하지 않아 Astro 전체 빌드는 이 작업환경에서 완료하지 못했습니다. 배포 환경에서 `npm install` 후 `npm run build`를 실행해 최종 확인해 주세요.
